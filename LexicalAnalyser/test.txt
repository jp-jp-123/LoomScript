x = 10

if x > 5:
    OUTPUT("x is greater than 5")
    if x < 15:
        OUTPUT("x is also less than 15")
    else:
        OUTPUT("x is greater than or equal to 15")
else:
    OUTPUT("x is less than or equal to 5")
